require("ibl").setup {
    -- for example, context is off by default, use this to turn it on
    -- show_trailing_blankline_indent = false,
    -- use_treesitter = true,
    scope = {
        show_start = false,
        show_end = false,
    }
}
